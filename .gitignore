# ROMs — never commit (copyright). Users provide their own.
roms/*
!roms/.gitkeep
*.gb
*.gbc
*.gba

# Emulator save states
*.state
*.sav

# Agent run outputs (screenshots, logs, trajectories)
runs/

# Agent memory notebook — per-savegame runtime state, not part of the repo
memory/

# Throwaway test screenshots written to the repo root
test_frame.png
before.png
after.png
vision_test.png

# Python
__pycache__/
*.py[cod]
.ipynb_checkpoints/
.venv/
venv/

# Secrets / local config
.env
*.local

# Internal handoff / working notes (not part of the public repo)
HANDOFF.md

# OS
.DS_Store
Thumbs.db
