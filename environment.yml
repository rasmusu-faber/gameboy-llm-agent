name: gameboy-agents
channels:
  - conda-forge
dependencies:
  - python=3.11
  - pip
  - pip:
      - pyboy
      - pillow
      - httpx          # LLM HTTP calls (local Ollama API + OpenAI-compatible cloud)
      - ollama         # local model server client (still used to pull/run models)
